# Snapshot placeholder — generate the first real snapshot with:
#   pnpm --filter @cadence/api run schema:emit && cp apps/api/schema.graphql packages/graphql/schema.public.graphql
type Query { _placeholder: Boolean }
